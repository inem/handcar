TODO:
  Client-side:
    - remove time
    - change "Handcar:" to something shorter (some UTF symbol?)
  Server-side
    - aliases 'console.log' and 'cl'
    - honk should accept multiple attributes, ie "honk(a,b,3,[1,2,{:a+>4}], ...)"
    - implement additional methods for console.info, console.error, group and so on ?
